# 开发环境配置
# @yutiansut @quantaxis

# WebSocket 服务器地址 (DIFF 协议)
# ✨ 使用相对路径，通过 Vue devServer 代理转发
VUE_APP_WS_URL=/ws/diff

# 默认订阅的合约（逗号分隔）
# ✨ 添加 CFFEX.IF2501 以支持K线数据展示 @yutiansut @quantaxis
VUE_APP_DEFAULT_INSTRUMENTS=CFFEX.IF2501,SHFE.cu2501,SHFE.ag2506,CFFEX.IH2501

# API 基础 URL
# ✨ 使用相对路径，通过 Vue devServer 代理转发到后端 8094 端口
# 这样无论从 localhost 还是 192.168.x.x 访问前端都能正常工作
VUE_APP_API_BASE_URL=/api
