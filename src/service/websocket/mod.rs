//! WebSocket 服务

pub struct WebSocketServer {}

impl WebSocketServer {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for WebSocketServer {
    fn default() -> Self {
        Self::new()
    }
}
